using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enum{
    public enum Behave_all{
        feed,
        wash,
        cleaning,
        playing
    }

    public enum Behave_chick{
        egg,
    }

    public enum Behave_duck{
        egg,
        swimming,
    }

    public enum Behave_goat{
        milk,
    }

    public enum Behave_pig{
        dirt,
        fruits
    }
    public enum Behave_horse{
        fruits,
        walk,
        race,
        ride
    }

    public enum Behave_cow{
        dig,
        fight

    }
}